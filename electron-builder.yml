appId: com.grove.app
productName: Grove
directories:
  buildResources: build
  output: dist
files:
  - out/**
  - package.json
mac:
  category: public.app-category.productivity
  target:
    - dmg
    - zip
  # Grove reaches the Apple apps and reads the frontmost window title, so macOS
  # requires these usage strings before it will show the permission prompts.
  extendInfo:
    NSAppleEventsUsageDescription: >-
      Grove uses Calendar, Reminders, Notes and Mail on your Mac to read and
      create items on your behalf.
    NSCalendarsUsageDescription: >-
      Grove reads and creates events in your calendar.
    NSRemindersUsageDescription: >-
      Grove reads and creates your reminders.
    LSUIElement: false
  hardenedRuntime: true
  gatekeeperAssist: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
dmg:
  artifactName: Grove-${version}-${arch}.dmg
